export enum Topic {
    START = 'start',
    STOP = 'stop',
    CONFIG = 'config',
    FEATURES = 'features',
    STATUS = 'status',
    STATS = 'stats'
  }
  
  export enum EventType {
    SUBSCRIBE = 'subscribe',
    UNSUBSCRIBE = 'unsubscribe',
    ENDPOINT = 'endpoint',
    FEED = 'feed',
  }